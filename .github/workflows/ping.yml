name: Ping (schedule test)

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Print UTC time
        run: date -u +"UTC %Y-%m-%d %H:%M:%S"

